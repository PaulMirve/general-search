export default class User {
    constructor(name, lastName, birthday, department, photo) {
        this.name = name;
        this.lastName = lastName;
        this.birthday = birthday;
        this.department = department;
        this.photo = photo;
    }
}